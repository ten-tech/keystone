{
  "$id": "https://keystone.invalid/schema/workstation.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Keystone — workstation.yaml",
  "description": "Le fichier d'état désiré, tel qu'il est écrit et relu.\n\nLa forme est celle décidée par l'ADR-0010 : un en-tête, une table plate\nindexée par chemin d'item, une liste d'écarts acceptés.",
  "type": "object",
  "properties": {
    "acceptedDrift": {
      "description": "Les écarts volontairement tolérés, avec leur échéance.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/EcartAccepte"
      }
    },
    "apiVersion": {
      "description": "Version du format, `keystone/v1`.",
      "type": "string",
      "const": "keystone/v1"
    },
    "desired": {
      "description": "Ce qui est voulu, une ligne par chemin d'item, tel que `ks scan` les nomme. Toute chaîne est une clé valide : le référentiel des chemins est ce qu'un scan a observé, jamais une liste figée.",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/ScalaireDeclare"
      },
      "default": {}
    },
    "kind": {
      "description": "Genre du document, `Workstation`.",
      "type": "string",
      "const": "Workstation"
    },
    "metadata": {
      "description": "De quelle machine il s'agit, et de quoi elle hérite.",
      "$ref": "#/$defs/Metadata"
    }
  },
  "additionalProperties": false,
  "required": [
    "apiVersion",
    "kind",
    "metadata"
  ],
  "$defs": {
    "DesirDabsence": {
      "description": "Le désir que cet item n'existe pas sur la machine.",
      "type": "object",
      "properties": {
        "absent": {
          "description": "Toujours `true`. Le champ porte le sens ; sa valeur ne fait que le confirmer.",
          "type": "boolean",
          "const": true
        }
      },
      "additionalProperties": false,
      "required": [
        "absent"
      ]
    },
    "EcartAccepte": {
      "description": "Un écart toléré volontairement, et jusqu'à quand.\n\nExigence D2-06 : la raison **et** la date d'expiration sont obligatoires, et\nc'est le typage qui le tient — aucun des deux champs n'est optionnel. C'est\nle seul mécanisme qui empêche un fichier d'état de pourrir sous une couche\nd'exceptions dont personne ne se rappelle le motif.",
      "type": "object",
      "properties": {
        "decidedAt": {
          "description": "Quand la décision a été prise.",
          "type": "string",
          "format": "date-time"
        },
        "decidedBy": {
          "description": "Qui a décidé.",
          "type": "string"
        },
        "expires": {
          "description": "Quand la tolérance expire, et l'écart redevient actif tout seul.",
          "type": "string",
          "format": "date"
        },
        "item": {
          "description": "Le chemin de l'item toléré.",
          "type": "string"
        },
        "reason": {
          "description": "Pourquoi cet écart est toléré.",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "item",
        "reason",
        "expires",
        "decidedBy",
        "decidedAt"
      ]
    },
    "Metadata": {
      "description": "De quelle machine parle le document.",
      "type": "object",
      "properties": {
        "description": {
          "description": "À quoi il sert, en une phrase.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "inherits": {
          "description": "Surcouche de flotte dont ce fichier hérite (D2-10, D13-01).",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "name": {
          "description": "Nom de la machine.",
          "type": "string"
        },
        "owner": {
          "description": "Qui tient ce poste.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "additionalProperties": false,
      "required": [
        "name"
      ]
    },
    "ScalaireDeclare": {
      "description": "La valeur voulue pour un item : un booléen, un nombre, un texte, une liste de textes, ou `{ absent: true }` pour exiger que l'item n'existe pas. Un texte s'entoure de guillemets — sans eux, YAML lit `off`, `no` et `0x9` comme un booléen ou un nombre.",
      "anyOf": [
        {
          "description": "`{ absent: true }` — l'item ne doit pas exister.",
          "$ref": "#/$defs/DesirDabsence"
        },
        {
          "description": "`true` ou `false`, y compris ce que YAML retype depuis `off`, `no`, `on`.",
          "type": "boolean"
        },
        {
          "description": "Un nombre entier.",
          "type": "integer",
          "format": "int64"
        },
        {
          "description": "Un texte : jeton, version, chemin, numéro de série.",
          "type": "string"
        },
        {
          "description": "Une liste de textes.",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      ]
    }
  }
}
